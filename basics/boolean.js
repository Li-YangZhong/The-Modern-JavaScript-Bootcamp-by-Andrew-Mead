let temp = 180
// let isFreezing = false
// let isFreezing = temp === 31
// let isFreezing = temp !== 32
// let isFreezing = temp < 32
// let isFreezing = temp > 32
// let isFreezing = temp >= 32
let isFreezing = temp <= 32
// let isFreezing = temp === 32
console.log(isFreezing)

if (isFreezing) {
    console.log('It is freezing outside1')
}

if (temp >= 110) {
    console.log('It is way too hot outside')
}

let age = 5

if (age <= 7) {
    console.log('discount for children applied')
}
if (age >= 65) {
    console.log('discount for seniors applied')
}